"use client";

import EmptySection from "@/components/menu/EmptySection";

export default function AccountPage() {
  return (
    <EmptySection
      title="Mi Cuenta"
      description1="-"
      description2="-"
      buttonText="-"
      onClick={() => console.log("-")}
    />
  );
}
