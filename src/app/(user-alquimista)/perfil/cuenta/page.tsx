"use client";

import SeccionVacia from "@/components/menu/EmptySection";

export default function FormulasPage() {
  return (
    <SeccionVacia
      titulo="Mi Cuenta"
      descripcion1="-"
      descripcion2="-"
      textoBoton="-"
      onClick={() => console.log("-")}
    />
  );
}
